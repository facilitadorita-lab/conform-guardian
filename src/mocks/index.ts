// Central de dados mockados do Conform Flow.
// Enquanto VITE_USE_MOCKS=true, todos os services leem daqui.
// Quando o backend Supabase estiver pronto, basta alternar a flag em runtime-config.
export * from "./conformflow-mocks";